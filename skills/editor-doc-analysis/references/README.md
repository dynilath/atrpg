# references

本目录用于存放「按需加载」的参考文档——**不会**自动进入主对话上下文，仅按路径引用（对应 Claude Skills 的 references/ 语义）。

可放内容示例：

- `sample-analysis-report.md` — analyze_upload 报告的输出样例（供人工校准格式）
- `index-format.md` — index.json 字段说明
- 调试记录、回归用例

当前为空，按需补充。
